# -------------------------------
# TROJANCHAT BACKEND DEPENDENCIES
# -------------------------------

# Web framework
fastapi==0.110.0

# ASGI server
uvicorn[standard]==0.29.0

# Environment variable management
python-dotenv==1.0.1

# Data validation / models
pydantic==2.6.4
pydantic-settings==2.2.1

# Optional: for Firebase realtime database (future upgrade)
firebase-admin==6.3.0

# Optional: async support (recommended)
httpx==0.27.0

# Optional utilities for production readiness
loguru==0.7.2

# For UUIDs and timestamps, the Python stdlib is enough — no external package needed.
