FROM python:3.12-slim

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -r requirements.txt

RUN useradd -m -u 10000 trojanchat && chown -R trojanchat:trojanchat /app
USER trojanchat

EXPOSE 10000

CMD ["uvicorn", "backend.api:app", "--host", "0.0.0.0", "--port", "10000"]