# ===========================================
#      TROJANCHAT BACKEND ENV TEMPLATE
#   (No real secrets — safe to commit)
# ===========================================

# ------------ GENERAL SETTINGS -------------
DEBUG=True
HOST=0.0.0.0
PORT=8000

# ------------ FIREBASE (Optional) ----------
# These fields support future real-time message syncing
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_PROJECT_ID=trojanchat-project
FIREBASE_DB_URL=https://your-project-id.firebaseio.com

# Toggle between WebSockets vs Firebase
USE_FIREBASE=True

# ------------ AUTHENTICATION ---------------
# For JWT (future)
JWT_SECRET=your_jwt_secret_here
JWT_ALGORITHM=HS256

# ------------ DATABASE (Optional Future) ---
POSTGRES_URL=postgresql://user:password@localhost:5432/trojanchat
